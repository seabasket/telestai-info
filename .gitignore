# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Dependencies (unused today, but keep out of the repo if ever installed)
node_modules/
vendor/
.bundle/

# OS / editor cruft
.DS_Store
*.swp
*.swo
*~
